import {CalendarDays,HeartPulse,Home,LogOut,MessageCircle,NotebookTabs,WifiOff} from 'lucide-react'
export type Page='home'|'calendar'|'medicines'|'records'|'assistant'
const nav=[['home',Home,'Home'],['calendar',CalendarDays,'Calendar'],['medicines',HeartPulse,'Medicines'],['records',NotebookTabs,'Records'],['assistant',MessageCircle,'Follow ups']] as const
export function Layout({page,setPage,onLogout,children}:{page:Page;setPage:(p:Page)=>void;onLogout:()=>void;children:React.ReactNode}){const appName=import.meta.env.VITE_APP_NAME||'Patient Care';return <><header><div className="brand"><span className="brand-mark">+</span>{appName}</div><button className="logout" onClick={onLogout} aria-label="Sign out"><LogOut/><span>Sign out</span></button></header>{!navigator.onLine&&<div className="offline" role="status"><WifiOff/> You are offline. Information may not be current.</div>}<main>{children}</main><nav className="bottom-nav" aria-label="Main navigation">{nav.map(([id,Icon,label])=><button key={id} aria-current={page===id?'page':undefined} onClick={()=>setPage(id)}><Icon/><span>{label}</span></button>)}</nav></>}
